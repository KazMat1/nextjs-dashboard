// for search
const QUERY_PARAMS: {
    query: 'query',
    page: 'page'
} = {
    query: 'query',
    page: 'page',
}
const DEBOUNCED_SECONDS: number = 300;

// for pagination
const INITIAL_PAGE_NUMBER: number = 1;

const INVOICE_INDEX_PATH = '/dashboard/invoices';
export { QUERY_PARAMS, DEBOUNCED_SECONDS, INITIAL_PAGE_NUMBER, INVOICE_INDEX_PATH };
